<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de red -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permisos para instalar APKs -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <!-- Storage legacy -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android TV -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- Touch no obligatorio -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <queries>
        <package android:name="org.acestream.node.web" />
        <package android:name="org.free.aceserve" />
    </queries>

    <application
        android:name=".AceStreamTVApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_tv"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_tv_round"
        android:banner="@drawable/atv_banner"
        android:logo="@drawable/atv_banner"
        android:supportsRtl="true"
        android:theme="@style/Theme.ASLTV"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">

            <!-- Launcher móvil -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Launcher Android TV -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />

                <!-- Hace que Android TV la detecte como app de vídeo -->
                <category android:name="android.intent.category.APP_VIDEO" />
            </intent-filter>

            <!-- ===================================================== -->
            <!-- VIDEOS HTTP / HTTPS -->
            <!-- ===================================================== -->

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="http"
                    android:mimeType="video/*" />

                <data
                    android:scheme="https"
                    android:mimeType="video/*" />
            </intent-filter>

            <!-- ===================================================== -->
            <!-- HLS (.m3u8) -->
            <!-- ===================================================== -->

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="http"
                    android:mimeType="application/x-mpegurl" />

                <data
                    android:scheme="https"
                    android:mimeType="application/x-mpegurl" />

                <data
                    android:scheme="http"
                    android:mimeType="application/vnd.apple.mpegurl" />

                <data
                    android:scheme="https"
                    android:mimeType="application/vnd.apple.mpegurl" />
            </intent-filter>

            <!-- ===================================================== -->
            <!-- MPEG DASH -->
            <!-- ===================================================== -->

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="http"
                    android:mimeType="application/dash+xml" />

                <data
                    android:scheme="https"
                    android:mimeType="application/dash+xml" />
            </intent-filter>

            <!-- ===================================================== -->
            <!-- ARCHIVOS LOCALES -->
            <!-- ===================================================== -->

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:scheme="file"
                    android:mimeType="video/*" />

                <data
                    android:scheme="content"
                    android:mimeType="video/*" />
            </intent-filter>

            <!-- EXTENSIONES COMUNES -->

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="http"
                    android:host="*"
                    android:pathPattern=".*\\.mp4" />

                <data
                    android:scheme="https"
                    android:host="*"
                    android:pathPattern=".*\\.mp4" />

                <data
                    android:scheme="http"
                    android:host="*"
                    android:pathPattern=".*\\.mkv" />

                <data
                    android:scheme="https"
                    android:host="*"
                    android:pathPattern=".*\\.mkv" />

                <data
                    android:scheme="http"
                    android:host="*"
                    android:pathPattern=".*\\.m3u8" />

                <data
                    android:scheme="https"
                    android:host="*"
                    android:pathPattern=".*\\.m3u8" />

                <data
                    android:scheme="http"
                    android:host="*"
                    android:pathPattern=".*\\.mpd" />

                <data
                    android:scheme="https"
                    android:host="*"
                    android:pathPattern=".*\\.mpd" />
            </intent-filter>

        </activity>

        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- WorkManager -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>